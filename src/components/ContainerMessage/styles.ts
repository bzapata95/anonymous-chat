import styled from 'styled-components';

import ScrollToBottom from 'react-scroll-to-bottom';

export const Container = styled(ScrollToBottom)`
  padding: 24px;
  height: 100%;

  overflow: hidden;
  padding-bottom: 60px;
  overflow-x: hidden;
`;
